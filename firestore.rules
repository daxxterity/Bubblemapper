rules_version = '2';
// Version: 1.0.10 - Global permissive rules for mapper database
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
